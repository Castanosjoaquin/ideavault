/**
 * Tipos generados por Supabase CLI desde el schema remoto.
 * Regenerar con: pnpm supabase gen types typescript --linked > packages/core/src/supabase/database.types.ts
 *
 * Placeholder hasta que haya tablas creadas en Fase 1.
 */
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
    CompositeTypes: Record<string, never>;
  };
};
